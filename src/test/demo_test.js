const mocha = require("mocha");
const assert = require("assert");

//mieu ta qua trinh kiem tra
describe('Mieu ta demo', () => {
    //tao ra test de kiem tra
    it('kiem tra tong hai so', () => {
        assert(1 + 1  === 2);
    });
    
});
